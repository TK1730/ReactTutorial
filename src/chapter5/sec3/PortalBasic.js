import { useState } from "react";
import { createPortal } from "react-dom";
// import './PortralBasic.css';

export default function PortalBasic() {
    // ダイアログの開閉状態を表すState (falseで閉じた状態)
    const [show, setShow] = useS
}
